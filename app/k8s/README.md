Currently, This k8s `temporal-service.yaml` hardcoded to apply service to Temporal web that installed with Helm named `temporaltest` please update the `selector` to the value your use when run Helm install.